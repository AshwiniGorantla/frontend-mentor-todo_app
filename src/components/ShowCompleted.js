import React from "react";

const ShowCompleted = ({selectedList}) => {
    return (
        <div>
            {/* {selectedList.map((item)=>{
            <span>{item}</span> })} */}
            <span>Test</span>
        </div>
    )
}

export default ShowCompleted;